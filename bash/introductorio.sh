#!/bin/bash

# echo -e "\a de esta forma podemos emitir una alerta al momento de que se ejecute una cadena"

# Para ejecutar cualquier script de shell, primero debemos indicar el interprete el cual deseamos utilizar, esto lo hacemos mediante "#!/bin/bash" con este comentario, le estamos indicando que interprete debe utilizar para ejecutar al mokento de ejecutar el script.

# Lo segundo que se debe realizar es darle los permisos necesarios de ejecución al script. Esto lo realizamos mediante le comando chmod, este comando recibe como primer argumento los permisos que desemos darle al archivo, de segundo el archivo al cual queremos darle los permisos. Como atajo podemos darle permisos de ejecución mediante "chmod u+x [ruta_archivo]".